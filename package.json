{
  "name": "codex-mobile-remote",
  "private": true,
  "license": "MIT",
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "build": "npm --workspace apps/web run build",
    "check": "npm --workspace apps/web run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
